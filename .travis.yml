language: cpp
sudo: required
dist: trusty
compiler:
- clang
os:
- linux
before_install:
  - sudo apt-get update
  - sudo apt-get install -y python3 libboost-all-dev eigen
install:
  - mkdir build
  - cd build
  - cmake ..
  - make install


